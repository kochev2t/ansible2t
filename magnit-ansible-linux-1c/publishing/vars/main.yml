---
# vars file for publishing

enablade_client_pablic: # Начальное заполнение запрета публикации клиенского приложения в default.vrd

web_services: # Начальное определение переменной - выходной строки по web сервисам в default.vrd
ws_enabled: # Начальное определение подстроки в default.vrd включения веб серверов
ws_enabled_def: # Начальное определение подстроки в default.vrd включения веб серверов по умолчанию

http_services: # Начальное пустое определение переменной - выходной строки по http сервисам в default.vrd
http_enabled_def: # Начальное определение подстроки в default.vrd включения http серверов по умолчанию
